import { ScrollView } from "react-native";
import React, { Component } from "react";

export default class ProfileList extends Component {
  render() {
    return <ScrollView className="bg-black"></ScrollView>;
  }
}
